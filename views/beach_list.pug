extends layout

block content
  h1= title
  
  ul
    each beach in beach_list
      li 
        a(href=beach.url) #{beach.beach_name} - #{beach.location}

    else
      li There are no beaches on the database.